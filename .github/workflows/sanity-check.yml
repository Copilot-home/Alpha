name: Sanity Check

on:
  push:
    branches: [ "**" ]
  pull_request:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "CI runner is alive"

  with-checkout:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Echo
        run: echo "checkout worked"
